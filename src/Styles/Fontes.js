import { StyleSheet } from "react-native";

const Fontes = StyleSheet.create({
    tituloPrimaria: {
        fontSize: 24,
        color: '#000',
    },
    
    tituloSecundaria: {
        fontSize: 24,
        color: '#fff',
    },

    escritaPrimaria: {
        fontSize: 16,
        color: '#000',
    },

    escritaSecundaria: {
        fontSize: 16,
        color: '#fff',
    },
});

export default Fontes;